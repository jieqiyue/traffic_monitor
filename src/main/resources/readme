1. 由于要提高性能，所以将packet对象进行了复用。在ParsePcapFile类中，由每次都创建新的转变为一个static的对象。只会创建一次。
所以带来的问题是上一个数据包的数据会残留到下一个。所以对于network这一层来说是没有影响的，因为它判断自己是否能够解析是通过Ethernet层
的数据来进行标识的，而这个数据会在每一次都重新被赋值。因为ehternet层每一次都会进。
而现在要避免transport层进行了非法的解析，比如说上一个包是udp包，那么此时packet.ipv4TransProtocolCH字段是"UDP"。那么如果这个包
是ARP包的话，它在解析完毕之后就不能把它往上传了。否则又会去解析UDP，而这个包不是UDP包，这样就会出错。所以此时需要将ARP包在解析完成之后返回
false，如果返回了false，那么后续解析就会中断。
那么这个packet.ipv4TransProtocolCH字段为UDP会不会对后续造成影响呢。是不会的。因为后续如果是ipv4的话，这个字段会被重新覆盖。
2. 由于ipv6上层协议以及ipv6都没有解析，所以如果碰到ipv6就直接跳过了。